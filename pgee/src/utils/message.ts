//消息提示函数
/*export const showMessage = (message: string, type: 'success' | 'error' = 'error') => {
  // 这里可以使用更专业的UI库如的消息组件
  // 暂时使用简单的alert，后续替换为UI库的提示
  if (type === 'error') {
    alert(`错误: ${message}`)
  } else {
    alert(`成功: ${message}`)
  }
}

export const createMessageDialog = showMessage
*/
